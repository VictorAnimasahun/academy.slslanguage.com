# Block all direct HTTP access to this directory.
# Slide assets here are served only through resources/protected_viewer/serve_slide.php,
# which checks login + subscription tier per request before streaming a file.
Order deny,allow
Deny from all
